export default class Choice {
  constructor(id, name, timestamp, additionalInfo) {
    this.id = id;
    this.name = name;
    this.timestamp = timestamp;
    this.additionalInfo = additionalInfo;
  }
}
